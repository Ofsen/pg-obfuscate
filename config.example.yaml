seed: 12345
tables:
  # Tables default to 'public' schema. Use 'schema.table' for other schemas.
  users:
    email: fake:email
    username: fake:name
    phone: null
    created_at: fake:datetime
  orders:
    order_total: fake:decimal
    tax_amount: null
    status: preserve
  profiles:
    height: fake:number
